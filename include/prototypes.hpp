#ifndef PROTOTYPES_HPP
# define PROTOTYPES_HPP

//int	set_nonblocking(int fd);

//int	create_listen_socket(const std::string &host, int port);

//int	parse_request_line(const std::string &raw, t_request_line &out, size_t &consumed);

#endif
